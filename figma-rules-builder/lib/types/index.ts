/**
 * Type Definitions Index
 *
 * Central export point for all TypeScript types
 */

export type {
  FigmaNode,
  FigmaNodeType,
  LayoutMode,
  Paint,
  Color,
  Effect,
  Rectangle,
  Constraints,
} from './figma';

export type { AltNode, AltNodeType, CSSProperties } from './altnode';

export type {
  MappingRule,
  Selector,
  Transformer,
  RuleMatch,
  ConflictSeverity,
  ResolvedProperties,
} from './rule';

export type {
  GeneratedCode,
  CodeFormat,
  CodeMetadata,
} from './generated-code';
